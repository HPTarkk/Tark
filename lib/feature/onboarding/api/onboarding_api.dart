/// Public surface of the onboarding feature.
///
/// Only the app composition root (router) needs anything from here; other
/// features reach the onboarding scene via AppRoutes, never by importing it.
library;

export '../presentation/page/onboarding_page.dart';
